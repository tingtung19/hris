@extends('layouts.app')
@section('title','Recruitment')
@section('content')
<h1 class="h3 mb-3">Recruitment</h1>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<div class="card shadow-sm mb-3"><div class="card-body"><form method="POST" action="{{ route('recruitment.vacancies.store') }}" class="row g-2">@csrf<input name="title" class="form-control" placeholder="Judul lowongan" required><select name="employment_type" class="form-select"><option value="contract">Kontrak</option><option value="permanent">Tetap</option><option value="intern">Magang</option><option value="daily">Harian</option><option value="freelance">Freelance</option></select><input name="quota" type="number" min="1" value="1" class="form-control"><select name="status" class="form-select"><option value="open">Dibuka</option><option value="on_hold">Ditunda</option><option value="closed">Ditutup</option></select><input name="posted_date" type="date" class="form-control"><input name="closing_date" type="date" class="form-control"><textarea name="description" class="form-control" placeholder="Deskripsi"></textarea><textarea name="requirements" class="form-control" placeholder="Persyaratan"></textarea><button class="btn btn-primary">Tambah lowongan</button></form></div></div>
<div class="card shadow-sm"><div class="card-body"><table class="table"><thead><tr><th>Lowongan</th><th>Tipe</th><th>Kuota</th><th>Status</th><th></th></tr></thead><tbody>@foreach($vacancies as $vacancy)<tr><td>{{ $vacancy->title }}<div class="small text-muted">{{ $vacancy->posted_date ?: '-' }} s/d {{ $vacancy->closing_date ?: '-' }}</div></td><td>{{ ucfirst($vacancy->employment_type) }}</td><td>{{ $vacancy->quota }}</td><td>{{ ucfirst($vacancy->status) }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('recruitment.pipeline',$vacancy->id) }}">Pipeline</a><form method="POST" action="{{ route('recruitment.vacancies.destroy',$vacancy->id) }}" class="d-inline">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus lowongan ini?')">Hapus</button></form></td></tr>@endforeach</tbody></table>{{ $vacancies->links() }}</div></div>
@endsection
