@extends('layouts.app')
@section('title','Cuti')
@section('content')
<h1 class="h3 mb-3">Leave Management</h1>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif
<div class="row g-3"><div class="col-lg-4"><div class="card shadow-sm"><div class="card-body"><h2 class="h5">Ajukan cuti</h2><form method="POST" action="{{ route('leave.store') }}">@csrf<select class="form-select mb-2" name="leave_type_id" required><option value="">Pilih tipe</option>@foreach($types as $type)<option value="{{ $type->id }}">{{ $type->name }}</option>@endforeach</select><input class="form-control mb-2" type="date" name="start_date" required><input class="form-control mb-2" type="date" name="end_date" required><textarea class="form-control mb-2" name="reason" placeholder="Alasan"></textarea><button class="btn btn-primary">Kirim pengajuan</button></form></div></div></div>
<div class="col-lg-8"><div class="card shadow-sm"><div class="card-body"><h2 class="h5">Pengajuan saya</h2><table class="table"><thead><tr><th>Tipe</th><th>Periode</th><th>Hari</th><th>Status</th><th></th></tr></thead><tbody>@forelse($requests as $row)<tr><td>{{ $row->leave_type_name }}</td><td>{{ $row->start_date }} - {{ $row->end_date }}</td><td>{{ $row->total_days }}</td><td>{{ ucfirst($row->status) }}</td><td>@if(in_array($row->status,['pending','approved']))<form method="POST" action="{{ route('leave.cancel',$row->id) }}">@csrf<button class="btn btn-sm btn-outline-danger">Batalkan</button></form>@endif</td></tr>@empty<tr><td colspan="5" class="text-center text-muted">Belum ada pengajuan.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection
